<% if @volunteers.any? %>
  <ul>
    <% @volunteers.each do |project| %>
      <li><a href="/project/<%= project.id %>"><%= project.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no volunteers to display.</p>
<% end %>